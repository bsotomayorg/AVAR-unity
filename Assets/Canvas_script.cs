using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Canvas_script : MonoBehaviour{
    /*NOT WORKING. DELETE (bsotomayor. Apr 16th)*/
    // Start is called before the first frame update
    private CanvasGroup trans;
    public float alphaValue = 1;
    void Start(){
        //trans = GetComponent<CanvasGroup>();
        //trans.alpha = alphaValue;
    }

    // Update is called once per frame
    void Update() {
        //this.GetComponent<MeshRenderer>().material.color = new Color(1.0f, 1.0f, 1.0f, 0.5f);
        //this.GetComponent<CanvasGroup>().alpha = 0.5f;
    }
}
